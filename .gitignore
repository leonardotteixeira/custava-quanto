.venv/
__pycache__/
*.pyc

# Dados brutos da ANP (por posto revendedor): muito grandes (~1GB+) e são
# apenas um cache de download. Regenere com scripts/download_anp.py.
data/raw/

# Os agregados processados (pequenos, CSV) ficam versionados propositalmente
# em data/processed/, para reprodutibilidade e para quem clonar o repo poder
# rodar a análise sem precisar baixar tudo de novo.

.ipynb_checkpoints/
data/anp_download*.log
.claude/
# Design resources (external repos)
design-resources/
